import { Navigation } from "./Navigation"

export const Header = () => {
    return (
        <header>
            <Navigation />
            <h1 className="text-center py-3">Rocket</h1>
        </header>
    )
}